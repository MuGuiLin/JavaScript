// import * as all from "./module-2.js";

// console.dir(all)

/*
let count = 666;

export const PI = Math.PI;

const random = Math.random();

const sum = (n1, n2) => {
    return n1 + n2;
};

export function isOdd(n) {
    return n % 2 === 1 || n % 2 === -1;
};

export class Person {
    constructor(name) {
        this.name = name;
    }
    sayHello() {
        console.log(`Hello！ 我是${this.name}`);
    }
};

export default  {
    count: count,
    random,
    sum,
};
/**
 * 可以默认导出： 变量、常量、函数、对象、类 等对象
 */